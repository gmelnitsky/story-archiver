[pytest]
# CI runs a bare `pytest`, whose console-script entrypoint does NOT put the
# repo root on sys.path. `pythonpath = .` makes `import scrape` / `build_site`
# / `build_portable` resolve from the project root regardless of how pytest is
# invoked.
pythonpath = .
testpaths = tests
